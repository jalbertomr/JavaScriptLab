// Operadores Especiales
/*
    Conditional operator
    Comma operator
    delete
    in
    instanceof
    new
    this
    typeof
    void
*/

//Conditional operator
var edades = [4,15,18,20];

for( edad of edades){
  status = edad <= 18 ? 'menor' : 'mayor';
    console.log(status);
}
